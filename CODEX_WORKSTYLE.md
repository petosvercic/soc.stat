# CODEX_WORKSTYLE

- Always produce one PowerShell script per change batch (no fragments).
- Always Set-Location to repo root at script start.
- Always use explicit file paths for write operations.
- No secret keys or env values committed.
